"""QML-facing Python data models (Phase 9A+).

The models in this package are ``QObject`` / ``QAbstractListModel`` subclasses
exposed to QML via ``qmlRegisterType``.  They form the typed boundary between
the engine / strategy layer and the Qt Quick scene graph.
"""
