# Strategy service runtime dependencies
#
# All packages here are headless (no Qt, no GUI).  The C++
# orderflow_engine extension is built at image build time via
# backtestingCpp/orderflow/build.sh and is loaded from
# backtestingCpp/orderflow/build/.

fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
msgpack==1.1.0
redis==5.2.1
numpy==2.2.1
pandas==2.2.3
h5py==3.12.1
websockets==14.1
python-dotenv==1.0.1

# Backtesting / strategy code dependencies (existing platform modules).
scipy==1.14.1
