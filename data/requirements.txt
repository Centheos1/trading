# Data collection service dependencies — headless (no Qt).
#
# This is a superset of the legacy requirements-collector.txt: it adds
# Redis + msgpack for publishing raw market data to the strategy
# service.

websockets>=12.0,<15
requests>=2.31,<3
h5py>=3.10,<4
numpy>=1.26,<2.3
pandas>=2.1,<3
python-binance>=1.0.19,<2
oandapyV20>=0.6,<1
python-dotenv>=1.0,<2
pybind11>=2.11,<3
boto3>=1.34,<2
pyarrow>=15,<20
s3fs>=2024.2,<2025
pytz>=2024.1

# Distributed pipeline
redis>=5.0,<6
msgpack>=1.0,<2
